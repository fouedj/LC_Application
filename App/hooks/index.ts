import withError from './withError';
import {getFavoriteBookmarks} from './useBookmark';
import {addFavoriteBookmark} from './useBookmark';
import {removeFavoriteBookmark} from './useBookmark';
import {removeFavoriteBookmarks} from './useBookmark';
import {getLocalFavorites} from './useBookmark';
import {
  getSavedSearches,
  addSearchSaved,
  deleteSavedSearch,
  addNotifSavedSearch,
  removeNotifSavedSearch
} from './useSavedSearch';

export {
  withError,
  getFavoriteBookmarks,
  addFavoriteBookmark,
  removeFavoriteBookmark,
  removeFavoriteBookmarks,
  getLocalFavorites,
  getSavedSearches,
  addSearchSaved,
  deleteSavedSearch,
  addNotifSavedSearch,
  removeNotifSavedSearch
};
